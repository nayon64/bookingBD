/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["www.istockphoto.com", "i.pcmag.com", "images.pexels.com"],
  },
};


export default nextConfig;
